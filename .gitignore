# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editors / IDE
.vscode/
.idea/
*.swp
*~

# Claude Code local settings (per-user, not for the repo)
.claude/

# Node / build (app/ has its own too; catch-all here)
node_modules/
dist/

# 展示用サンプルの書き出し（8MB超。app/bake-samples.ts でいつでも再生成できる）
samples/

# Logs & temp
*.log
.cache/

# Secrets / env (誤コミット防止。クライアント側でも秘密情報は置かない)
.env
.env.*
!.env.example
*.pem
*.key
secrets.json
